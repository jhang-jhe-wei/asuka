<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :狀態, :class=>'lbl_normal' %><br />
    <%= form.text_field :state, :class=>'tbx_normal' %>
  </div>

  <div class="field">
    <%= form.label :使用者編號, :class=>'lbl_normal' %><br />
    <%= form.text_field :user_id, :class=>'tbx_normal' %>
  </div>

  <div class="field">
    <%= form.label :產品編號, :class=>'lbl_normal' %><br />
    <%= form.text_field :product_id, :class=>'tbx_normal' %>
  </div>

  <div class="field">
    <%= form.label :數量, :class=>'lbl_normal' %><br />
    <%= form.text_field :quantity, :class=>'tbx_normal' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
